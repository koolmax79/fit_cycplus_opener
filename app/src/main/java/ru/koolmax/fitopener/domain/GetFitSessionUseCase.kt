package ru.koolmax.fitopener.domain

class GetFitSessionUseCase(private val fitLibrary : IFitLibrary) {
    //fun execute(fit: String): FitSession? {
    //    return fitLibrary.GetFit(fit).GetSession()
    //}
}