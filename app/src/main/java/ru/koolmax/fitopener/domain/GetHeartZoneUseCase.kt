package ru.koolmax.fitopener.domain

class GetHeartZoneUseCase(private val fitLibrary: IFitLibrary, private val heartZone: IHeartZone) {
    //fun execute(fit: String) = fitLibrary.GetFit(fit).GetHeartZone(heartZone)
}